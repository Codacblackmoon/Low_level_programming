#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	if (n < 0);
		printf("%d is positive\n", n);
	eles if (n==0);
		printf("%d is zero\n", n);
	eles
		printf("%d is negative\n", n);
	return (0);
}
