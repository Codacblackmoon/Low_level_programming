#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char wrod;

	for (word == 'a'; word <= 'z'; word++)
		putchar(word);

	for (word == 'A'; word <= 'Z'; word++)
		putchar(word);

			putchur('\n');
		return (0);
}
