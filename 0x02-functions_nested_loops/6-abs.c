#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @n: the int in question
 *
 * Return: the ads of the int.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
