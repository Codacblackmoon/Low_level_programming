#include "main.h"
#include <stdio.h>

/**
 * _abs - main function
 * @b: input
 * Return: 0
 */
int _abs(int b)
{
         if (b < 0)
         {
                 return (-b);
         }
         else
         {
		return (b);
	 }
}
